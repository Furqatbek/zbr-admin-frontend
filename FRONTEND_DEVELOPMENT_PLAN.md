# 🏗️ План разработки Frontend Admin Panel

## Обзор проекта

**Цель:** Создание современной, интуитивной административной панели для системы доставки еды.

**Технологический стек:**
- **Framework:** React 18+ с TypeScript
- **Build Tool:** Vite 5
- **Стилизация:** TailwindCSS 3 + shadcn/ui
- **Состояние:** Zustand + TanStack Query (React Query)
- **Маршрутизация:** React Router v6
- **Формы:** React Hook Form + Zod
- **Графики:** Recharts
- **Таблицы:** TanStack Table
- **WebSocket:** Socket.io-client или native WebSocket + STOMP
- **HTTP Client:** Axios
- **Контейнеризация:** Docker + Docker Compose

---

## 📦 БЛОК 1: Инициализация проекта

### 1.1 Структура проекта
```
src/
├── api/                    # API клиенты и запросы
│   ├── axios.ts           # Конфигурация Axios
│   ├── auth.api.ts        # Эндпоинты авторизации
│   ├── users.api.ts       # Эндпоинты пользователей
│   ├── couriers.api.ts    # Эндпоинты курьеров
│   ├── restaurants.api.ts # Эндпоинты ресторанов
│   ├── orders.api.ts      # Эндпоинты заказов
│   ├── dashboard.api.ts   # Эндпоинты дашборда
│   ├── analytics.api.ts   # Эндпоинты аналитики
│   ├── notifications.api.ts # Эндпоинты уведомлений
│   └── settings.api.ts    # Эндпоинты настроек
├── components/
│   ├── ui/                # Базовые UI компоненты (shadcn/ui)
│   ├── layout/            # Layout компоненты
│   ├── shared/            # Общие компоненты
│   └── features/          # Функциональные компоненты
├── hooks/                  # Кастомные хуки
├── lib/                    # Утилиты и хелперы
├── pages/                  # Страницы приложения
├── store/                  # Zustand stores
├── types/                  # TypeScript типы
├── locales/                # Русская локализация
└── websocket/              # WebSocket клиент
```

### 1.2 Конфигурационные файлы

**Файлы для создания:**
- `vite.config.ts` - конфигурация Vite
- `tailwind.config.js` - конфигурация TailwindCSS
- `tsconfig.json` - конфигурация TypeScript
- `.env.example` - пример переменных окружения
- `Dockerfile` - образ для production
- `docker-compose.yml` - оркестрация контейнеров

---

## 📦 БЛОК 2: Аутентификация

### 2.1 Страницы
| Страница | Путь | Описание |
|----------|------|----------|
| Вход | `/login` | Форма входа в систему |

### 2.2 Компоненты
- `LoginForm` - форма входа (email/телефон + пароль)
- `AuthGuard` - защита маршрутов
- `RoleGuard` - проверка ролей (ADMIN, PLATFORM)

### 2.3 Store (Zustand)
```typescript
interface AuthStore {
  user: User | null;
  accessToken: string | null;
  refreshToken: string | null;
  isAuthenticated: boolean;
  login: (credentials) => Promise<void>;
  logout: () => void;
  refreshTokens: () => Promise<void>;
}
```

### 2.4 API Интеграция
| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/auth/login` | Вход в систему |
| POST | `/auth/refresh` | Обновление токена |
| GET | `/auth/me` | Получение текущего пользователя |

### 2.5 Функциональность
- ✅ Автоматическое обновление токена
- ✅ Сохранение сессии в localStorage
- ✅ Перенаправление неавторизованных пользователей
- ✅ Валидация полей формы

---

## 📦 БЛОК 3: Layout и Навигация

### 3.1 Компоненты Layout
- `MainLayout` - основной layout с sidebar
- `Sidebar` - боковое меню навигации
- `Header` - верхняя панель с профилем
- `Breadcrumbs` - хлебные крошки

### 3.2 Структура меню навигации
```
📊 Главная (Dashboard)
👥 Пользователи
   └─ Список пользователей
   └─ Роли и права
🚴 Курьеры
   └─ Все курьеры
   └─ Верификация
   └─ Карта курьеров
🍽️ Рестораны
   └─ Все рестораны
   └─ Заявки на модерацию
📦 Заказы
   └─ Все заказы
   └─ Проблемные заказы
📈 Аналитика
   └─ Доходы
   └─ Заказы
   └─ Операции
   └─ Финансы
   └─ Клиентский опыт
   └─ Безопасность (только ADMIN)
   └─ Технические метрики
🔔 Уведомления
   └─ Рассылка
   └─ Очистка
⚙️ Настройки (только ADMIN)
   └─ Платформа
   └─ Экспорт данных
```

### 3.3 Адаптивность
- Desktop: полный sidebar
- Tablet: сворачиваемый sidebar
- Mobile: выдвижное меню (drawer)

---

## 📦 БЛОК 4: Дашборд (Главная страница)

### 4.1 Страница
| Страница | Путь | Описание |
|----------|------|----------|
| Дашборд | `/` | Обзорная панель |

### 4.2 Виджеты
1. **Карточки метрик (KPI)**
   - Всего заказов
   - Общая выручка
   - Активные пользователи
   - Активные рестораны
   - Активные курьеры
   - Ожидают подтверждения

2. **График выручки** (линейный график)

3. **Таблица "Застрявшие заказы"**
   - ID заказа, ресторан, время ожидания, статус

4. **Таблица "Отменённые заказы"**
   - Причины отмен, статистика

5. **Пиковые часы** (тепловая карта или гистограмма)

6. **Производительность ресторанов** (топ-5)

7. **Производительность курьеров** (топ-5)

8. **Здоровье системы**
   - Статус сервисов
   - Время отклика API

### 4.3 API Интеграция
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/admin/dashboard/overview` | Обзорные метрики |
| GET | `/admin/dashboard/orders/stuck` | Застрявшие заказы |
| GET | `/admin/dashboard/orders/canceled` | Отменённые заказы |
| GET | `/admin/dashboard/peak-hours` | Пиковые часы |
| GET | `/admin/dashboard/restaurants/performance` | Производительность ресторанов |
| GET | `/admin/dashboard/couriers/performance` | Производительность курьеров |
| GET | `/admin/dashboard/revenue` | Выручка |
| GET | `/admin/dashboard/support/summary` | Саммари поддержки |
| GET | `/admin/dashboard/system/health` | Здоровье системы |

### 4.4 Real-time обновления (WebSocket)
- Подписка на `/topic/orders/new`
- Подписка на `/topic/orders/stuck`
- Подписка на `/topic/system/alerts`

---

## 📦 БЛОК 5: Управление пользователями

### 5.1 Страницы
| Страница | Путь | Описание |
|----------|------|----------|
| Список пользователей | `/users` | Таблица пользователей |
| Детали пользователя | `/users/:id` | Профиль пользователя |

### 5.2 Компоненты
- `UsersTable` - таблица с пагинацией, фильтрами, сортировкой
- `UserDetailsCard` - карточка с информацией о пользователе
- `UserStatusBadge` - бейдж статуса (ACTIVE, INACTIVE, SUSPENDED)
- `RoleAssignmentModal` - модальное окно назначения ролей
- `UserActionsDropdown` - dropdown с действиями

### 5.3 Фильтры
- По роли (CONSUMER, RESTAURANT_OWNER, COURIER, ADMIN, PLATFORM)
- По статусу (ACTIVE, INACTIVE, SUSPENDED)
- Поиск по имени/email/телефону

### 5.4 Действия
- Просмотр деталей
- Изменение статуса
- Блокировка/разблокировка аккаунта
- Назначение роли (только ADMIN)
- Удаление роли (только ADMIN)
- Удаление пользователя (только ADMIN)

### 5.5 API Интеграция
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/users` | Список пользователей |
| GET | `/users/{id}` | Детали пользователя |
| PUT | `/users/{id}/status` | Изменение статуса |
| POST | `/users/{id}/lock` | Блокировка |
| POST | `/users/{id}/unlock` | Разблокировка |
| DELETE | `/users/{id}` | Удаление |
| POST | `/users/{id}/roles` | Назначение роли |
| DELETE | `/users/{id}/roles/{role}` | Удаление роли |

---

## 📦 БЛОК 6: Управление курьерами

### 6.1 Страницы
| Страница | Путь | Описание |
|----------|------|----------|
| Список курьеров | `/couriers` | Таблица курьеров |
| Детали курьера | `/couriers/:id` | Профиль курьера |
| Верификация | `/couriers/verification` | Очередь на верификацию |
| Карта курьеров | `/couriers/map` | Карта с позициями |

### 6.2 Компоненты
- `CouriersTable` - таблица курьеров
- `CourierDetailsCard` - карточка курьера
- `CourierStatusBadge` - бейдж статуса
- `VerificationQueue` - очередь на верификацию
- `CourierMap` - карта с курьерами (Yandex Maps / Leaflet)
- `CourierVerificationModal` - модальное окно верификации

### 6.3 Фильтры
- По статусу (PENDING_APPROVAL, AVAILABLE, BUSY, OFFLINE)
- По верификации (верифицирован/не верифицирован)

### 6.4 Действия
- Просмотр деталей
- Верификация курьера
- Поиск курьеров рядом (для ручного назначения)

### 6.5 API Интеграция
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/couriers` | Список курьеров |
| GET | `/couriers/{id}` | Детали курьера |
| POST | `/couriers/{id}/verify` | Верификация |
| GET | `/couriers/nearby` | Поиск рядом |

### 6.6 Real-time обновления (WebSocket)
- Подписка на `/topic/couriers/status`

---

## 📦 БЛОК 7: Управление ресторанами

### 7.1 Страницы
| Страница | Путь | Описание |
|----------|------|----------|
| Список ресторанов | `/restaurants` | Таблица ресторанов |
| Детали ресторана | `/restaurants/:id` | Профиль ресторана |
| Модерация | `/restaurants/moderation` | Заявки на модерацию |

### 7.2 Компоненты
- `RestaurantsTable` - таблица ресторанов
- `RestaurantDetailsCard` - карточка ресторана
- `RestaurantStatusBadge` - бейдж статуса
- `ModerationQueue` - очередь модерации
- `RestaurantOrdersTable` - заказы ресторана
- `StatusChangeModal` - модальное окно смены статуса

### 7.3 Фильтры
- По статусу (PENDING, APPROVED, SUSPENDED, REJECTED)
- По состоянию (открыт/закрыт)

### 7.4 Действия
- Просмотр деталей
- Одобрение/отклонение заявки
- Приостановка ресторана
- Просмотр заказов ресторана

### 7.5 API Интеграция
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/restaurants` | Список ресторанов |
| GET | `/restaurants/{id}` | Детали ресторана |
| PUT | `/restaurants/{id}/status` | Изменение статуса |
| GET | `/restaurants/{id}/orders` | Заказы ресторана |

---

## 📦 БЛОК 8: Управление заказами

### 8.1 Страницы
| Страница | Путь | Описание |
|----------|------|----------|
| Все заказы | `/orders` | Таблица заказов |
| Детали заказа | `/orders/:id` | Информация о заказе |
| Проблемные заказы | `/orders/issues` | Заказы с проблемами |

### 8.2 Компоненты
- `OrdersTable` - таблица заказов с фильтрами
- `OrderDetailsCard` - полная информация о заказе
- `OrderStatusBadge` - бейдж статуса заказа
- `OrderTimeline` - временная шкала заказа
- `OrderItemsList` - список позиций заказа
- `OrderStatusChangeModal` - модальное окно изменения статуса

### 8.3 Фильтры
- По статусу заказа
- По ресторану
- По клиенту
- По курьеру
- По дате (от/до)

### 8.4 Действия
- Просмотр деталей
- Изменение статуса (с указанием причины)
- Отмена заказа

### 8.5 API Интеграция
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/orders` | Список заказов |
| GET | `/orders/{id}` | Детали заказа |
| PUT | `/orders/{id}/status` | Изменение статуса |

### 8.6 Real-time обновления (WebSocket)
- Подписка на `/topic/orders/new`
- Подписка на `/topic/orders/stuck`

---

## 📦 БЛОК 9: Аналитика

### 9.1 Страницы
| Страница | Путь | Описание |
|----------|------|----------|
| Доходы | `/analytics/revenue` | Аналитика доходов |
| Заказы | `/analytics/orders` | Аналитика заказов |
| Пользователи | `/analytics/users` | Рост пользователей |
| Рестораны | `/analytics/restaurants` | Аналитика ресторанов |
| Операции | `/analytics/operations` | Операционная аналитика |
| Финансы | `/analytics/financial` | Финансовая аналитика |
| Клиентский опыт | `/analytics/cx` | Удовлетворённость клиентов |
| Безопасность | `/analytics/fraud` | Аналитика фрода (ADMIN) |
| Технические | `/analytics/technical` | Технические метрики |

### 9.2 Общие компоненты
- `DateRangePicker` - выбор периода
- `GroupBySelector` - группировка (день/неделя/месяц)
- `ChartCard` - карточка с графиком
- `MetricCard` - карточка с метрикой
- `DataTable` - таблица данных

### 9.3 Графики
- Линейные графики (тренды)
- Столбчатые диаграммы (сравнение)
- Круговые диаграммы (распределение)
- Тепловые карты (пиковые часы)

### 9.4 API Интеграция

**Общая аналитика:**
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/analytics/revenue` | Доходы |
| GET | `/analytics/orders` | Заказы |
| GET | `/analytics/users` | Пользователи |
| GET | `/analytics/restaurants` | Рестораны |

**Операционная аналитика:**
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/analytics/operations/delivery-times` | Время доставки |
| GET | `/analytics/operations/order-volume` | Объём заказов |
| GET | `/analytics/operations/courier-utilization` | Загрузка курьеров |
| GET | `/analytics/operations/restaurant-preparation` | Время подготовки |

**Финансовая аналитика:**
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/analytics/financial/revenue` | Доходы |
| GET | `/analytics/financial/payouts` | Выплаты |
| GET | `/analytics/financial/commissions` | Комиссии |
| GET | `/analytics/financial/refunds` | Возвраты |

**Клиентский опыт:**
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/analytics/cx/satisfaction` | Удовлетворённость |
| GET | `/analytics/cx/retention` | Удержание |
| GET | `/analytics/cx/complaints` | Жалобы |

**Безопасность (только ADMIN):**
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/analytics/fraud/suspicious-orders` | Подозрительные заказы |
| GET | `/analytics/fraud/payment-anomalies` | Аномалии платежей |
| GET | `/analytics/fraud/account-anomalies` | Аномалии аккаунтов |
| GET | `/analytics/fraud/promo-abuse` | Злоупотребление промо |
| GET | `/analytics/fraud/velocity-checks` | Проверки частоты |

**Технические метрики:**
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/analytics/technical/api-performance` | Производительность API |
| GET | `/analytics/technical/error-rates` | Частота ошибок |
| GET | `/analytics/technical/websocket-metrics` | Метрики WebSocket |

---

## 📦 БЛОК 10: Уведомления

### 10.1 Страницы
| Страница | Путь | Описание |
|----------|------|----------|
| Рассылка | `/notifications/broadcast` | Создание рассылки |
| Очистка | `/notifications/cleanup` | Управление очисткой |

### 10.2 Компоненты
- `BroadcastForm` - форма создания рассылки
- `RoleSelector` - выбор целевых ролей
- `PrioritySelector` - выбор приоритета
- `CleanupActions` - действия по очистке

### 10.3 Функциональность
- Массовая рассылка по ролям
- Выбор приоритета (LOW, MEDIUM, HIGH)
- Очистка просроченных уведомлений
- Очистка отклонённых уведомлений
- Очистка прочитанных уведомлений

### 10.4 API Интеграция
| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/notifications/broadcast` | Отправка рассылки |
| POST | `/notifications/admin/cleanup/expired` | Очистка просроченных |
| POST | `/notifications/admin/cleanup/dismissed` | Очистка отклонённых |
| POST | `/notifications/admin/cleanup/read` | Очистка прочитанных |

---

## 📦 БЛОК 11: Настройки системы (только ADMIN)

### 11.1 Страницы
| Страница | Путь | Описание |
|----------|------|----------|
| Настройки платформы | `/settings` | Конфигурация системы |
| Экспорт данных | `/settings/export` | Экспорт отчётов |

### 11.2 Компоненты
- `PlatformSettingsForm` - форма настроек платформы
- `CommissionSettings` - настройки комиссий
- `DeliveryFeeSettings` - настройки стоимости доставки
- `OrderLimitsSettings` - лимиты заказов
- `RegionsSettings` - настройки регионов
- `FeatureFlags` - переключатели функций
- `ExportForm` - форма экспорта данных

### 11.3 Функциональность
- Изменение комиссии платформы
- Настройка диапазона стоимости доставки
- Настройка лимитов заказов
- Управление регионами
- Включение/отключение функций
- Экспорт данных в CSV

### 11.4 API Интеграция
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/admin/settings` | Получение настроек |
| PUT | `/admin/settings` | Обновление настроек |
| POST | `/analytics/export` | Экспорт данных |

---

## 📦 БЛОК 12: WebSocket интеграция

### 12.1 Подключение
```typescript
// websocket/client.ts
const WEBSOCKET_URL = 'ws://localhost:8080/ws';

// STOMP подключение с авторизацией
const stompClient = new Client({
  brokerURL: WEBSOCKET_URL,
  connectHeaders: {
    Authorization: `Bearer ${accessToken}`
  }
});
```

### 12.2 Топики для подписки
| Топик | Описание | Использование |
|-------|----------|---------------|
| `/topic/orders/new` | Новые заказы | Dashboard, Orders |
| `/topic/orders/stuck` | Застрявшие заказы | Dashboard, Orders |
| `/topic/couriers/status` | Статус курьеров | Couriers, Map |
| `/topic/system/alerts` | Системные алерты | Header, Dashboard |

### 12.3 Хуки
- `useWebSocket` - управление подключением
- `useSubscription` - подписка на топик
- `useRealTimeOrders` - реал-тайм заказы
- `useRealTimeCouriers` - реал-тайм курьеры

### 12.4 Уведомления в UI
- Toast-уведомления для важных событий
- Звуковые сигналы для критических алертов
- Badge с количеством новых событий

---

## 📦 БЛОК 13: Docker конфигурация

### 13.1 Dockerfile
```dockerfile
# Multi-stage build
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine AS production
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### 13.2 docker-compose.yml
```yaml
version: '3.8'
services:
  admin-frontend:
    build: .
    ports:
      - "3000:80"
    environment:
      - VITE_API_URL=http://api:8080/api/v1
      - VITE_WS_URL=ws://api:8080/ws
    depends_on:
      - api
    networks:
      - app-network

  # API service (если нужен для разработки)
  # api:
  #   image: backend-api:latest
  #   ports:
  #     - "8080:8080"
  #   networks:
  #     - app-network

networks:
  app-network:
    driver: bridge
```

### 13.3 nginx.conf
```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://api:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /ws {
        proxy_pass http://api:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
    }
}
```

---

## 📦 БЛОК 14: Локализация (Русский язык)

### 14.1 Структура локализации
```
src/locales/
├── ru/
│   ├── common.json      # Общие фразы
│   ├── auth.json        # Авторизация
│   ├── users.json       # Пользователи
│   ├── couriers.json    # Курьеры
│   ├── restaurants.json # Рестораны
│   ├── orders.json      # Заказы
│   ├── analytics.json   # Аналитика
│   ├── notifications.json # Уведомления
│   └── settings.json    # Настройки
└── index.ts             # Экспорт
```

### 14.2 Примеры переводов

**common.json:**
```json
{
  "actions": {
    "save": "Сохранить",
    "cancel": "Отмена",
    "delete": "Удалить",
    "edit": "Редактировать",
    "view": "Просмотр",
    "search": "Поиск",
    "filter": "Фильтр",
    "export": "Экспорт",
    "refresh": "Обновить"
  },
  "status": {
    "active": "Активен",
    "inactive": "Неактивен",
    "suspended": "Приостановлен",
    "pending": "Ожидает",
    "approved": "Одобрен",
    "rejected": "Отклонён"
  },
  "pagination": {
    "showing": "Показано",
    "of": "из",
    "entries": "записей",
    "previous": "Назад",
    "next": "Далее"
  }
}
```

---

## 📦 БЛОК 15: UI/UX Рекомендации

### 15.1 Цветовая схема
- **Primary:** Синий (#3B82F6) - основные действия
- **Success:** Зелёный (#10B981) - успех, одобрение
- **Warning:** Оранжевый (#F59E0B) - предупреждения
- **Danger:** Красный (#EF4444) - ошибки, удаление
- **Neutral:** Серый (#6B7280) - текст, фон

### 15.2 Типографика
- Заголовки: Inter/Nunito Sans Bold
- Текст: Inter/Nunito Sans Regular
- Размеры: 12px, 14px, 16px, 18px, 24px, 32px

### 15.3 Компоненты
- Закруглённые углы (8px)
- Тени для карточек
- Анимации переходов (200ms)
- Состояния hover/active/disabled
- Skeleton loading

### 15.4 Доступность
- Контрастность текста WCAG AA
- Фокус-кольца для клавиатурной навигации
- ARIA-метки для screen readers
- Подписи к иконкам

---

## 📅 Порядок разработки

### Фаза 1: Основа (Приоритет: Высокий)
1. ✅ Инициализация проекта (БЛОК 1)
2. ✅ Аутентификация (БЛОК 2)
3. ✅ Layout и Навигация (БЛОК 3)
4. ✅ Docker конфигурация (БЛОК 13)

### Фаза 2: Ядро (Приоритет: Высокий)
5. ✅ Дашборд (БЛОК 4)
6. ✅ Управление пользователями (БЛОК 5)
7. ✅ Управление заказами (БЛОК 8)

### Фаза 3: Расширение (Приоритет: Средний)
8. ✅ Управление курьерами (БЛОК 6)
9. ✅ Управление ресторанами (БЛОК 7)
10. ✅ WebSocket интеграция (БЛОК 12)

### Фаза 4: Аналитика (Приоритет: Средний)
11. ✅ Аналитика (БЛОК 9)

### Фаза 5: Дополнительно (Приоритет: Низкий)
12. ✅ Уведомления (БЛОК 10)
13. ✅ Настройки системы (БЛОК 11)
14. ✅ Локализация (БЛОК 14)

---

## 📋 Чеклист запуска

- [ ] Переменные окружения настроены
- [ ] API доступен и работает
- [ ] Docker образ собирается без ошибок
- [ ] Аутентификация работает
- [ ] WebSocket подключение работает
- [ ] Все страницы загружаются
- [ ] Локализация отображается корректно
- [ ] Адаптивность проверена на разных устройствах

---

## 🔧 Команды для разработки

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview

# Запуск линтера
npm run lint

# Запуск тестов
npm run test

# Docker сборка
docker build -t zbr-admin-frontend .

# Docker Compose запуск
docker-compose up -d
```

---

*Документ создан: 2024*
*Версия: 1.0*
